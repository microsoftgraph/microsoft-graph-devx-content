name: Validate sample queries
on: [pull_request,push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install 
        run: npm install

      - name: Run test
        run: npm run test
